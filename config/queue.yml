default: &default
  dispatchers:
    - polling_interval: 1
      batch_size: 500
  workers:
    # Importación PDF → Gemini: un hilo para no disparar la API en paralelo (cuota).
    - queues: pdf_import
      threads: 1
      processes: 1
      polling_interval: 0.1
    - queues: [ default, mailers ]
      threads: 3
      processes: <%= ENV.fetch("JOB_CONCURRENCY", 1) %>
      polling_interval: 0.1

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
