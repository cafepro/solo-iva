<% content_for :title, "Editar factura — SoloIVA" %>

<div class="mb-6 flex justify-end">
  <span class="font-mono text-sm text-gray-500"><%= @invoice.invoice_number %></span>
</div>

<%= render "form", invoice: @invoice %>
